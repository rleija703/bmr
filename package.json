{
  "private": true,
  "name": "bert-mortgage-rates",
  "version": "0.0.0",
  "license": "MIT",
  "contributors": [
    "ruben.leija@me.com",
    "jordan.cervantes1@me.com"
  ],
  "scripts": {
    "start": "if-env NODE_ENV=production && npm run -s serve || npm run -s dev",
    "build": "preact build",
    "serve": "preact build && sirv build --cors --single",
    "dev": "preact watch",
    "lint": "eslint src"
  },
  "eslintConfig": {
    "extends": "eslint-config-synacor"
  },
  "eslintIgnore": [
    "build/*"
  ],
  "devDependencies": {
    "@types/classnames": "^2.2.10",
    "css-loader": "^1.0.1",
    "eslint": "^6.0.1",
    "eslint-config-synacor": "^3.0.4",
    "if-env": "^1.0.0",
    "preact-cli": "^3.0.0-rc.6",
    "sirv-cli": "^0.4.5",
    "ts-loader": "^6.2.2",
    "typescript": "^3.8.3",
    "typings-for-css-modules-loader": "^1.7.0"
  },
  "dependencies": {
    "classnames": "^2.2.6",
    "preact": "^10.1.0",
    "preact-render-to-string": "^5.1.2"
  }
}
